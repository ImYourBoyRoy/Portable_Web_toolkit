# Templates (legacy path)

Starter files moved to **`../site-starter/`** — use that folder for new projects.

| Legacy file | Replacement |
|-------------|-------------|
| `package.astro-workers.json` | `site-starter/workers.package.json` → copy to `package.json` |
| `package.astro-static.json` | `site-starter/pages.package.json` → copy to `package.json` |
| `wrangler.toml` | `site-starter/workers.wrangler.toml` or `pages.wrangler.toml` |

See [site-starter/README.md](../site-starter/README.md).
