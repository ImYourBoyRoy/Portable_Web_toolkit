name = "[WORKER_NAME]"
compatibility_date = "2026-04-08"
compatibility_flags = ["nodejs_compat"]

[vars]
APP_ENV = "production"

# Add per-project bindings below (D1, R2, KV, send_email).
# Do not commit personal emails or production routes here for public toolkit repos.
# Prefer deploy-time routes: wrangler deploy --config dist/server/wrangler.json --route "example.com/*"
