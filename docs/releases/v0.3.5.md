# Portable Web Toolkit v0.3.5

Light skill defaults + freshest safe toolchain floors.

## Highlights

- **Token-light installs**: only `portable-web-toolkit-router` is `install_by_default`. Link heavy skills explicitly with `--skills`.
- **Site-starter floors** refreshed with `package-updater` (TypeScript capped at `^6.0.3` until `@astrojs/check` peers allow 7.x).
- **Node** pin / bootstrap tarballs → `26.7.0`.
- Focus-indicator probe scroll fix in bundled WCAG auditor.

## Operator notes

```bash
# Default link = router only
node ./scripts/manage-project-skills.mjs link --project /path/to/site

# Active toolkit site work
node ./scripts/manage-project-skills.mjs link --project /path/to/site \
  --skills portable-web-toolkit,site-readiness,site-starter,toolkit-update
```
