# Portable Web Toolkit v0.3.7

`.env` handoff: user owns API secrets; agent fills naming with the user; PostHog + GA4 explained early.

## Highlights

- **Sectioned `.env` template** (`site-starter/.env.example` + bootstrap generator):
  - **A** — Required APIs the user pastes (`CLOUDFLARE_API_TOKEN`, `GOOGLE_PAGESPEED_API_KEY`)
  - **B** — Agent proposes zone/worker/pages/URL with the user
  - **C** — Recommended **PostHog** + **GA4** (explain why in S5; wire in S7)
  - **D** — Optional features (forms, Instagram, registrar tooling)
- **Porkbun** is an optional registrar *example*, not a requirement.
- Agents must be curious: challenge vague names and “analytics later.”

## Operator notes

Follow `docs/agent-skills/ONBOARDING_STAGES.md` S5/S7 and copy `site-starter/.env.example` into new client projects.
