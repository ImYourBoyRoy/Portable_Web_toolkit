# Portable Web Toolkit v0.3.4

Keeps accessibility evidence **inside** the linked toolkit for website work.

## Highlights

- **`Web_Toolkit/wcag_auditor` is self-contained** — `core-path` resolves only to this module.
- Website agents must **not** open or patch `AI/wcag-auditor` (or any other tree under `AI/`).
- Includes Playwright `.default` browser interop fix for file-URL / CJS imports.

## Operator notes

1. Pull/update toolkit so the client’s `Web_Toolkit` link includes the full `wcag_auditor` tree (`src/cli.mjs` present).
2. Confirm: `node ./Web_Toolkit/wcag_auditor/bin/wcag-auditor.mjs core-path` ends with `Web_Toolkit/wcag_auditor`.
3. Optional peers in the **client** project: `playwright`, `@axe-core/playwright`.
