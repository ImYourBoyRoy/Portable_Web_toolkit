# Portable Web Toolkit v0.3.9

Hardening + full-product operator polish: fail-closed gates, glass/frost WCAG path, Astro Image defaults, Brand Guide → tokens.

## Highlights

- **Fail-closed doctors**: discovery-doctor, registrar MX gate, CF dry-run/`--apply` defaults, site-quality smoke compliance, louder toolkit-report/package-updater/project-init exits.
- **WCAG glassmorphism**: frost-aware remediations, `frost-glass-contrast` manual evidence, suppressions with `outcomes: ["cantTell"]` for axe incomplete contrast — keep frost UI, resolve evidence.
- **Astro Image posture**: site-starter `OptimizedPicture` + Workers `imageService: 'compile'`; image-pipeline audit/readiness checks.
- **package-updater**: runs official `npx @astrojs/upgrade` for Astro projects (dry-run / `--apply`), then registry pin updates.
- **Brand Guide → tokens**: `brand-doctor sync-tokens`; deeper stylesheet-check architecture rules.
- **Skills**: brand-doctor, discovery-doctor, image-pipeline, pagespeed-diagnostics, site-maintenance, wcag-auditor (+ site-maintenance routing).

## Notes

- Client reports stay under `<project>/output/`; toolkit self-checks under `Web_Toolkit/.runtime/`.
- Node pin lives at **repo root** `.node-version` (and `site-starter/.node-version` for client copies) — not under `Web_Toolkit/`.
- After `package-updater --apply` on Astro sites, still run `npm install` if the lockfile needs a refresh.
