# Not a client-site env template.
#
# Canonical sources:
#   - Client sites  → copy site-starter/.env.example → <site>/.env.example (then fill .env)
#   - Toolkit ops   → optional Web_Toolkit/.env.example (machine-level defaults only)
#
# Do not maintain a third full key list here — it will drift.
